# Berry & Ikea — Two-Sentence Story (Root version)
Images live in the repo root, so in `script.js` the paths are:
- `./berryikea_lock.jpg`
- `./berryikea_home.jpg`

Upload everything to your repo root and enable GitHub Pages. Open `index.html` locally to test.
